export enum AnimationDirection {
    NORMAL = "normal",
    REVERSE = "in reverse"
};

export const BBConsts = {

    // Mappings of the enums
    Direction_UI: [AnimationDirection.NORMAL, AnimationDirection.REVERSE]
}